UI Prototype — Unified Command Bar & Adaptive Layout

Open `index.html` in a browser to see a responsive prototype demonstrating:
- Unified Command Bar (keyboard + touch)
- Adaptive desktop / mobile layout
- Accessibility hooks (ARIA) and reduced-motion support

Run locally:

```bash
cd examples/ui-prototype
python3 -m http.server 8000
# then open http://localhost:8000
```
