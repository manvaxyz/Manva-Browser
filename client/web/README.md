# MANVA Web (React + Vite)

Quickstart:

1. cd client/web
2. npm install
3. npm run dev
4. Open http://localhost:5173

Notes:
- The frontend expects the AI agent to be reachable at http://127.0.0.1:8080
- Desktop shell can embed the web app (for development, run Vite and open the desktop executable).
