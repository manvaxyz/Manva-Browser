MANVA Desktop Shell (prototype)

This folder contains a minimal Rust prototype for the MANVA desktop shell.

Quick run (dev requires Rust toolchain):

```bash
cd client/desktop
cargo run
```

Replace this prototype with the renderer, UI process, and GPU compositor described in docs/architecture.md.
